# 快速开始指南

## 📋 项目配置总结

你的 SmartResume 项目已经配置完成！以下是所有创建的配置文件：

### 生成的文件结构
```
consume-optimizer/
├── src/
│   ├── App.jsx              ✅ 主应用组件（已配置）
│   ├── main.jsx             ✅ React 入口
│   └── index.css            ✅ 全局样式
├── index.html               ✅ HTML 入口
├── package.json             ✅ 项目依赖配置
├── vite.config.js           ✅ Vite 构建配置
├── tailwind.config.js       ✅ Tailwind CSS 配置
├── postcss.config.js        ✅ PostCSS 配置
├── .vscode/tasks.json       ✅ VS Code 任务配置
├── README.md                ✅ 项目文档
└── .gitignore               ✅ Git 忽略配置
```

## 🚀 快速开始（3步）

### 第1步：安装依赖

在终端中运行：
```bash
cd /Volumes/external/luming/consume-optimizer
npm install
```

或使用 yarn：
```bash
yarn install
```

⏱️ 预计时间：2-5 分钟（取决于网速）

### 第2步：配置 API Key

1. 访问 [Google AI Studio](https://aistudio.google.com/)
2. 点击 "Get API Key" 获取免费 API Key
3. 打开 `src/App.jsx`，找到第 26 行：
   ```jsx
   const apiKey = "";
   ```
4. 替换为你的 API Key：
   ```jsx
   const apiKey = "sk-xxxxxxxxxxxx";
   ```

> 💡 如果不配置 API Key，应用也能正常使用，只是 AI 优化功能无法使用。

### 第3步：启动开发服务器

在终端中运行：
```bash
npm run dev
```

✅ 浏览器会自动打开 `http://localhost:5173`

## 💻 使用 VS Code 任务运行（可选）

在 VS Code 中按下 `Ctrl+Shift+B`（macOS: `Cmd+Shift+B`），选择：
- **开发服务器 (dev)** - 启动开发服务器
- **构建生产版本 (build)** - 构建为生产版本
- **预览生产构建 (preview)** - 预览生产构建

## 📚 可用的 npm 命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查（如果需要）
npm run lint
```

## 🔧 依赖项说明

```json
{
  "dependencies": {
    "react": "^18.2.0",          // React 框架
    "react-dom": "^18.2.0",      // React DOM 渲染
    "lucide-react": "^0.263.1"   // 图标库
  },
  "devDependencies": {
    "vite": "^5.0.0",                      // 构建工具
    "@vitejs/plugin-react": "^4.1.0",      // React 插件
    "tailwindcss": "^3.3.0",               // CSS 框架
    "postcss": "^8.4.31",                  // CSS 后处理
    "autoprefixer": "^10.4.16"             // CSS 前缀自动添加
  }
}
```

## ✨ 项目特性预览

启动后你将看到：

### 左侧导航栏（4 个功能）
- 👤 **编辑模式** - 编辑简历内容和信息
- ✨ **AI 优化** - 使用 Gemini 优化简历
- 💼 **职位设置** - 设置目标 JD 进行定制化优化
- ⬇️ **导出 PDF** - 一键打印或导出为 PDF

### 右侧内容区域
- 📝 **实时编辑** - 左侧编辑，右侧实时预览
- 🎨 **3 种模板** - 切换专业风格
  - 现代蓝（科技公司）
  - 经典白（传统行业）
  - 极简紫（创意行业）

## 🐛 常见问题排查

### 问题1：npm install 失败
```bash
# 清除缓存后重试
npm cache clean --force
npm install
```

### 问题2：端口 5173 被占用
```bash
# 会自动使用下一个可用端口，或手动指定
npm run dev -- --port 3000
```

### 问题3：API Key 不工作
- 确保 Key 没有被暴露在版本控制中
- 检查 API 配额是否充足
- 尝试在 [Google Cloud Console](https://console.cloud.google.com/) 重新生成

### 问题4：样式不显示
```bash
# 重启开发服务器
npm run dev

# 或清除浏览器缓存后刷新
```

## 📱 浏览器兼容性

| 浏览器 | 版本 | 支持 |
|------|------|------|
| Chrome | 最新 | ✅ |
| Firefox | 最新 | ✅ |
| Safari | 最新 | ✅ |
| Edge | 最新 | ✅ |

## 🔐 安全提示

⚠️ **生产环境部署建议**：
1. 不要在代码中硬编码 API Key
2. 使用环境变量存储敏感信息
3. 在服务器端调用 API，不要在客户端

```bash
# 使用环境变量
echo "VITE_GEMINI_API_KEY=your-key" > .env.local
```

然后在 `App.jsx` 中：
```jsx
const apiKey = import.meta.env.VITE_GEMINI_API_KEY;
```

## 📞 获取帮助

- 查看 `README.md` 了解详细信息
- 检查 [Vite 文档](https://vitejs.dev/)
- 参考 [React 文档](https://react.dev/)
- 查看 [Tailwind CSS 文档](https://tailwindcss.com/)

## ✅ 下一步

1. ✅ 安装依赖 - `npm install`
2. ✅ 配置 API Key - 编辑 `src/App.jsx`
3. ✅ 启动服务器 - `npm run dev`
4. ✅ 开始编辑简历！

---

**祝你找到满意的工作！** 🎉
